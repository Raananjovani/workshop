pipeline {
    agent any
    stages {
        stage("verify python version") {
            steps {
                sh 'python3 --version
                    print("version is ok")'
            }
        }
        // stage("run python script") {
        //     steps {
        //         sh 'script.py'
        //     }
        // }
    }
}